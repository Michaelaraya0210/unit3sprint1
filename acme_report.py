import random
from Unit3Sprint1.acme import Product


def generate_products(num_products=30):
    product = []
for _ in list(range(num_products)):
    products = Product(self.identifier(Product, k=30))
